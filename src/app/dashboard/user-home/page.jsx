import MainContent from '@/components/dashboard/MainContent';
import React from 'react';

const page = () => {
    return (
        <MainContent>
            user home
        </MainContent>
    );
};

export default page;